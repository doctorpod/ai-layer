Check whether this KB or project has a `_wayfinder/` folder (see `_AI/local/workflows/wayfinder.md`), a `_greenhouse/` folder (see `_AI/shared/workflows/greenhouse.md`), a `radar/` folder (see `_AI/local/workflows/add-to-radar.md`), a `questions/` folder (see `_AI/shared/snippets/questions.md`), or any piece-folder — a `brief.md`, with or without `guide/` yet, see `_AI/shared/snippets/piece-folder.md`. If anything [CONTEXT] resolves, blocks, or informs an open wayfinder waypoint, is greenhouse-worthy (a concrete idea, deliberately parked for now), radar-worthy (a situation worth watching), plausibly relevant to a piece-folder's brief, or resolves/contradicts/bears on an open Question (`pending` or `surmised`), offer to update the relevant one before moving on — for a piece-folder, that means offering to run `_AI/local/workflows/sync-guide.md` on it (it builds `guide/` from scratch if one doesn't exist yet); for a Question, that means offering to update its `status` and `## Answer`. Offer separately per piece-folder if more than one exists.
