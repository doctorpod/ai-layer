Check whether this KB or project has a `_wayfinder/` folder (see `_AI/local/workflows/wayfinder.md`), a `_greenhouse/` folder (see `_AI/shared/workflows/greenhouse.md`), a `radar/` folder (see `_AI/local/workflows/add-to-radar.md`), or any piece-folder — a `brief.md`, with or without `guide/` yet, see `_AI/shared/snippets/piece-folder.md`. If anything [CONTEXT] resolves, blocks, or informs an open wayfinder waypoint, is greenhouse-worthy (a concrete idea, deliberately parked for now), radar-worthy (a situation worth watching), or plausibly relevant to a piece-folder's brief, offer to update the relevant one before moving on — for a piece-folder, that means offering to run `_AI/local/workflows/sync-guide.md` on it (it builds `guide/` from scratch if one doesn't exist yet). Offer separately per piece-folder if more than one exists.
